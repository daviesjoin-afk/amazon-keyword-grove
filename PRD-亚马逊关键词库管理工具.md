# 亚马逊多产品关键词库管理工具 PRD

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | 亚马逊多产品关键词库管理工具 |
| 文档版本 | V1.0 |
| 文档状态 | V1.1，已完成首份真实样表验证 |
| 主要数据源 | 卖家精灵“关键词反查”等 Excel/CSV 导出文件 |
| 主要用户 | 亚马逊运营、广告投手、产品负责人 |
| 首期形态 | 本地部署的桌面端友好 Web 应用 |

## 2. 产品背景

运营人员从卖家精灵反查一个或多个 ASIN 后，会得到数量较大的关键词表。原始表格适合数据查看，但难以持续完成多产品归档、语义分类、相关性判断、词根分析、跨产品复用和历史更新。

本产品需要把一次性的 Excel 数据转化为可长期维护的多产品关键词资产。用户可为每个产品建立独立词库，持续导入新的卖家精灵数据，并在统一界面中完成筛选、分类、搜索、编辑和导出。

## 3. 产品目标

### 3.1 核心目标

1. 支持多个产品分别建立和维护关键词库。
2. 兼容卖家精灵关键词反查表格的导入及版本变化。
3. 自动完成关键词清洗、去重、分类、相关性评分和排序。
4. 支持增量导入，保留历史数据及人工修改结果。
5. 支持按词根、意图、匹配强度及业务指标快速筛选。
6. 将筛选结果导出，用于 Listing、广告和运营分析。

### 3.2 成功指标

| 指标 | V1 验收目标 |
|---|---|
| 文件导入成功率 | 对约定的卖家精灵样表达到 100% |
| 关键词去重准确率 | 相同站点、标准化后相同关键词不重复建档 |
| 千行导入耗时 | 普通办公电脑不超过 10 秒，不含 AI 分析时间 |
| 人工结果保护 | 再次导入不得覆盖用户锁定的分类、标签和备注 |
| 可追溯性 | 任一关键词可查看首次发现、最近更新及来源批次 |
| 核心操作 | 产品创建、导入、筛选、编辑、删除、导出均可完成 |

## 4. 非目标

V1 暂不包含：

- 自动登录或抓取卖家精灵数据；
- 直接修改 Amazon Listing 或广告活动；
- 自动调价、自动竞价或自动否词；
- 多公司 SaaS 商业化计费；
- 用未经验证的 AI 结果直接替代人工运营判断；
- 实时关键词排名监控。

## 5. 用户角色

### 5.1 管理员

创建、归档和删除产品；管理分类规则、导入模板、备份及全局设置。

### 5.2 运营人员

导入文件、维护关键词、筛选词库、调整分类、添加备注并导出结果。

V1 可先采用单用户模式，但数据模型需预留创建人和修改人字段。

## 6. 核心业务对象

### 6.1 产品

每个产品拥有独立词库，建议字段如下：

- 产品名称；
- 自有 ASIN，可为空；
- 站点，如美国、日本、英国；
- 品牌；
- 产品类目；
- 产品标题；
- 五点描述；
- 产品描述/Search Terms，可选；
- 产品状态：准备中、在售、归档；
- 产品图片，可选；
- 创建时间、更新时间。

同一 ASIN 在不同站点视为不同产品。

### 6.2 竞品来源

一个产品可以关联多个被反查 ASIN：

- 竞品 ASIN；
- 竞品名称/备注；
- 数据来源；
- 首次导入时间；
- 最近导入时间；
- 导入批次数量。

### 6.3 关键词

关键词需要同时保存原始值、标准化值、卖家精灵指标和本工具生成的信息。

建议字段：

- 原始关键词；
- 标准化关键词；
- 所属站点和语言；
- 来源 ASIN；
- 来源文件及导入批次；
- 卖家精灵原始指标；
- 搜索量；
- 搜索量趋势或历史值，如源表提供；
- ABA 排名/搜索频率排名，如源表提供；
- 标题密度、商品数、供需比，如源表提供；
- PPC 竞价、点击集中度、转化相关指标，如源表提供；
- 自然排名、广告排名、流量占比等反查指标，如源表提供；
- 相关性得分；
- 匹配强度；
- 语义分类；
- 词根标签；
- 使用状态；
- 人工锁定标志；
- 备注；
- 首次发现和最近更新时间。

卖家精灵不同导出版本可能存在字段差异。未识别列必须保留在“原始扩展数据”中，不得静默丢弃。

## 7. 信息架构

### 7.1 产品中心

以卡片或表格展示全部产品，支持：

- 产品名称、ASIN、站点、关键词数量、强匹配数量、更新时间；
- 按名称、ASIN、站点、状态搜索和筛选；
- 新建、复制、归档、删除产品；
- 进入指定产品词库。

### 7.2 产品词库

产品详情页包含：

1. 数据概览；
2. 关键词列表；
3. 分类树；
4. 词根分析；
5. 导入记录；
6. 导出中心；
7. 产品资料和规则设置。

### 7.3 全局关键词中心

跨产品检索同一关键词或词根，查看其出现在哪些产品、竞品和导入批次中。V1 可只读，编辑仍在具体产品内进行。

## 8. 功能需求

### FR-01 产品管理

- 新建产品时，产品名称和站点必填。
- 支持录入标题、五点描述等相关性分析文本。
- 支持产品复制，但默认不复制关键词数据。
- 产品删除需二次确认，优先进入回收站。
- 回收站内容可恢复；彻底删除仅限管理员。

### FR-02 文件导入

支持 `.xlsx`、`.xls` 和 `.csv`。

导入流程：

1. 选择目标产品；
2. 上传一个或多个文件；
3. 识别工作表、表头、站点和来源 ASIN；
4. 展示字段映射预览；
5. 用户确认或修正映射；
6. 执行校验、清洗、去重和入库；
7. 展示导入结果报告。

导入报告至少包括：

- 总行数；
- 成功新增数；
- 已存在并更新数；
- 跳过数；
- 错误数及错误原因；
- 未识别字段；
- 来源 ASIN 统计。

系统应保存已确认的表头映射模板。后续遇到相同格式时自动匹配，但仍提供预览。

### FR-03 数据清洗与去重

标准化规则：

- 去除首尾空格；
- 合并连续空格；
- 英文统一为小写用于去重，界面保留原文；
- 统一常见标点和全角/半角字符；
- 空关键词不入库；
- 不擅自执行词干化，不把语义不同的单复数强行合并。

默认唯一键为“产品 + 站点 + 标准化关键词”。同一关键词来自多个 ASIN 时建立多条来源关系，而不是复制关键词主体。

### FR-04 增量更新

- 再次导入已有关键词时，更新最新卖家精灵指标。
- 同时保留关键指标的历史快照，支持后续趋势查看。
- 新来源 ASIN 追加到来源关系中。
- 人工分类、标签、备注、状态和锁定结果不得被自动覆盖。
- 每次导入作为独立批次，可查看明细并撤销本批次新增数据。
- 撤销批次不得删除此前已存在的关键词或其他批次来源。

### FR-05 自动分类

系统支持树形分类及多标签。一个关键词可有一个主分类和多个属性标签。

默认分类体系：

- 核心产品词；
- 产品属性词；
- 功能/用途词；
- 使用场景词；
- 受众词；
- 性别词；
- 年龄词；
- 颜色词；
- 尺寸/规格词；
- 材质词；
- 款式词；
- 季节/节日/年份词；
- 品牌词；
- 竞品词；
- 问题/痛点词；
- 泛词；
- 不相关词；
- 待确认。

分类要求：

- 自动分类必须保存分类依据及置信度；
- 低置信度进入“待确认”；
- 用户可批量改分类；
- 用户可新建、重命名、排序及停用分类；
- 人工结果可锁定；
- 用户纠错可以转化为该产品或全局规则。

### FR-06 相关性与匹配强度

相关性分析输入包括：产品标题、五点描述、产品描述、类目、人工核心词及排除词。

系统输出 0–100 的相关性得分及可解释原因。建议 V1 评分由可配置规则组成：

- 核心词/同义词命中；
- 产品属性命中；
- 标题和五点描述覆盖；
- 使用场景与受众匹配；
- 排除词或明显冲突属性命中；
- 品牌词、竞品词识别；
- 可选的语义相似度辅助分。

默认区间：

- 强匹配：80–100；
- 中匹配：50–79；
- 弱匹配：20–49；
- 不相关：0–19。

阈值必须可配置。搜索量只能影响排序优先级，不能单独决定相关性。

### FR-07 关键词列表

关键词表格支持：

- 固定关键词列；
- 自定义显示、隐藏和调整列顺序；
- 按搜索量、相关性、排名等多列排序；
- 分页或虚拟滚动；
- 单条和批量编辑；
- 批量分类、加标签、改状态、删除、导出；
- 查看原始数据、历史指标和全部来源。

### FR-08 搜索与筛选

支持以下组合条件：

- 关键词全文搜索；
- 包含/不包含某词根；
- 精确短语；
- 强、中、弱及不相关；
- 主分类和属性标签；
- 来源 ASIN；
- 搜索量、排名、竞价等数值范围；
- 已使用/待使用/观察/否定/不相关；
- 已人工修改/已锁定/待确认；
- 首次发现和最近更新时间；
- 导入批次。

用户可保存常用筛选视图。

### FR-09 词根分析

- 按空格拆分英文词组并统计词根出现频次；
- 展示含该词根的关键词数量、合计/平均搜索量及平均相关性；
- 点击词根立即筛选对应长尾词；
- 支持同义词组和排除词维护；
- 支持将词根批量设为核心、属性、品牌、否定或忽略；
- 词根统计需遵循当前筛选范围。

### FR-10 使用状态

每个关键词可设置：

- 待评估；
- Listing 已使用；
- 广告待投放；
- 广告已投放；
- 观察；
- 否定候选；
- 已否定；
- 不相关。

状态用于管理，不直接触发 Amazon 操作。

### FR-11 人工维护

- 支持新增自定义关键词；
- 支持编辑分类、标签、状态、匹配强度和备注；
- 支持软删除及恢复；
- 自动结果和人工结果需明确区分；
- 对人工锁定字段，任何重新分析或导入都不得覆盖；
- 批量修改前显示影响数量，操作后可撤销。

### FR-12 导出

支持导出当前产品全部词库或当前筛选结果，格式为 `.xlsx` 和 `.csv`。

预设导出模板：

- 完整关键词库；
- 强匹配核心词；
- Listing 埋词清单；
- 广告投放候选词；
- 否定词候选；
- 词根分析表；
- 来源 ASIN 对比表。

导出文件需记录产品、站点、筛选条件和导出时间。

### FR-13 导入历史与审计

- 查看每次导入的文件名、时间、目标产品、来源 ASIN 和处理结果；
- 查看人工操作日志；
- 可定位关键词由哪个批次首次加入；
- 支持下载错误明细；
- 导入原文件可配置保留或仅保存文件指纹。

### FR-14 数据备份

- 支持一键导出完整备份；
- 支持从备份恢复；
- 可配置自动备份周期和保留数量；
- 恢复前必须验证备份版本及完整性；
- 恢复操作不得静默覆盖当前数据，应先生成当前快照。

## 9. 排序优先级

系统提供一个可解释的“运营优先级”，用于排序而非替代决策。建议由以下指标组成：

- 相关性；
- 搜索需求；
- 竞争程度；
- 来源 ASIN 覆盖数；
- 当前自然/广告表现；
- 人工权重。

计算公式必须可配置并显示各分项，不允许只展示不可解释的综合分。

## 10. 关键页面

### 10.1 产品中心

顶部为全局搜索、新建产品和导入入口；主体展示产品列表与数据摘要。

### 10.2 新建/编辑产品

录入产品资料、Listing 文本、核心词、同义词和排除词。

### 10.3 导入向导

采用“上传文件 → 识别字段 → 数据预览 → 处理选项 → 结果报告”的五步流程。

### 10.4 产品词库页

左侧为分类树和已保存视图，中间为关键词表格，顶部为搜索和核心筛选，右侧抽屉展示关键词详情、来源、历史和编辑项。

### 10.5 词根分析页

左侧展示高频词根列表，右侧展示相关长尾词、指标分布和标签操作。

### 10.6 设置页

管理字段映射、分类体系、评分阈值、同义词、排除词、备份及数据保留策略。

## 11. 数据规则

### 11.1 来源优先级

- 原始指标以最新一次成功导入为当前值；
- 历史值不覆盖；
- 人工数据高于自动数据；
- 人工锁定高于任何重新计算；
- 删除采用软删除，除非执行明确的彻底删除。

### 11.2 跨产品隔离

- 每个产品拥有独立分类、状态、备注和相关性；
- 同一关键词可在全局层复用标准化结果和词根解析；
- 不得将 A 产品的“不相关”判断直接套用到 B 产品；
- 全局规则必须明确标记适用站点、语言和产品范围。

### 11.3 多站点和语言

- 产品必须归属站点；
- 去重、分词、同义词和分类规则按语言处理；
- 不同站点的数据不得默认合并；
- V1 优先支持 Amazon US 英文关键词，同时为其他站点预留能力。

## 12. 非功能需求

### 12.1 性能

- 单产品至少支持 10 万关键词；
- 全库至少支持 100 个产品、累计 100 万关键词；
- 普通筛选响应目标小于 1 秒；
- 大文件处理需显示进度，不阻塞界面。

### 12.2 稳定性

- 导入需事务化，失败时不得留下半批数据；
- 处理大文件时可安全重试；
- 关键写操作记录日志；
- 异常退出后数据库可恢复。

### 12.3 安全与隐私

- 默认本地存储，不将产品和关键词上传第三方；
- 如启用云端 AI，必须明确提示发送的数据范围并由用户开启；
- 日志不得记录账号密码、Cookie 或无关敏感信息；
- 备份文件应支持密码保护作为后续能力。

### 12.4 易用性

- 主要界面使用中文；
- 表格列名保留卖家精灵原名或提供对应说明；
- 所有自动判断均可人工修改；
- 危险操作需确认并尽量可恢复。

## 13. AI 使用原则

V1 可使用 AI 辅助分类和相关性判断，但需遵守：

1. 规则引擎优先处理确定性属性；
2. AI 仅处理难以规则化的语义任务；
3. 输出必须包含置信度和理由；
4. 批量调用需缓存，避免重复成本；
5. AI 失败时不影响原始数据导入；
6. 用户可完全关闭 AI，使用纯本地规则模式；
7. 未经确认不得把产品资料发送到外部模型。

## 14. MVP 范围

首个可用版本必须包含：

- 多产品创建、编辑、归档；
- 卖家精灵 Excel/CSV 导入向导；
- 字段映射、清洗、去重和增量更新；
- 产品独立关键词库；
- 搜索、筛选、排序和批量操作；
- 默认分类体系；
- 可解释的相关性评分和匹配强度；
- 词根搜索与长尾词查看；
- 人工编辑、锁定、软删除和恢复；
- 导入记录；
- Excel/CSV 导出；
- 本地备份和恢复。

## 15. 后续版本候选

- 产品之间的关键词交集、差集和复用分析；
- 多竞品覆盖矩阵；
- Listing 埋词检测；
- 广告搜索词报告导入；
- 关键词自然排名和广告排名趋势；
- 团队账号、权限和协作；
- 规则学习和分类质量评估；
- 卖家精灵 API 集成（需合法授权）；
- Amazon 广告 API 集成；
- 云端同步与多设备使用。

## 16. MVP 验收场景

### 场景 A：首次建立产品词库

1. 用户创建一个美国站产品并录入标题和五点描述。
2. 用户上传一份卖家精灵关键词反查表。
3. 系统正确识别或引导映射关键词、ASIN 和主要指标列。
4. 导入后关键词完成去重、分类、评分和排序。
5. 用户可以筛出强匹配、高搜索量关键词并导出。

### 场景 B：多竞品合并

1. 用户为同一产品继续导入其他竞品 ASIN 数据。
2. 已有关键词不重复创建，新增来源正确关联。
3. 页面可以按来源 ASIN 筛选，并统计一个关键词覆盖的竞品数。

### 场景 C：周期性更新

1. 用户一个月后导入新的卖家精灵表格。
2. 当前指标更新，旧指标保留为历史快照。
3. 用户此前修改并锁定的分类和备注不变。
4. 导入记录展示新增、更新、跳过和错误数量。

### 场景 D：多个产品隔离

1. 用户建立第二个产品并导入关键词。
2. 两个产品的数据、分类和状态互不污染。
3. 全局搜索可以看到一个关键词出现在哪些产品中。

### 场景 E：异常文件

1. 用户上传缺少关键词列或格式损坏的文件。
2. 系统阻止入库并说明原因。
3. 已有产品数据不受影响。

## 17. 开发前必须确认的输入

以下内容不阻塞 PRD 定稿，但会阻塞导入模块的最终验收：

1. 已收到并验证一份包含 20 个 ASIN 的真实卖家精灵关键词反查导出文件；后续如存在其他导出类型，可追加样表扩大兼容范围；
2. 用户实际使用的 Amazon 站点和语言；
3. 是否需要离线纯本地运行；
4. 是否允许调用外部 AI，及可使用的模型/API；
5. 期望保留的卖家精灵指标列；
6. 用户现有的分类习惯、核心词规则和否定词规则；
7. 单产品最大关键词量和预计产品数量。

## 18. 实施建议

建议按以下阶段交付：

1. 数据底座：多产品、数据库、导入映射、去重和导入记录；
2. 词库工作台：表格、搜索、筛选、批量维护和导出；
3. 智能分析：分类、词根、相关性、匹配强度和可解释评分；
4. 可靠性：历史快照、撤销、备份恢复、性能及异常测试；
5. 扩展分析：跨产品对比、Listing 埋词和广告数据。

当前字段字典以本次真实导出文件为基线，但仍不得把这一版列名写死为唯一格式；系统应通过别名和映射模板兼容卖家精灵后续版本及其他导出类型。

## 19. 真实样表分析基线（2026-08-27）

本 PRD 已使用一份脱敏的 SellerSprite 样例文件完成首次数据结构验证。源文件只读，不做修改；真实客户文件不纳入公开仓库。

### 19.1 文件结构

| 工作表 | 范围 | 用途 |
|---|---:|---|
| `US-SAMPLE(20)__` | A1:AG2001 | 主关键词数据，2,000 条、33 列 |
| `Unique Words` | A1:C1475 | 卖家精灵生成的词根、出现频次和百分比 |
| `Asin` | A1:A21 | 20 个被反查 ASIN |
| `Notes` | A1:A5 | 卖家精灵来源及数据备注 |

样表中的 2,000 个关键词均非空，按小写、去首尾空格后仍为 2,000 个唯一关键词。主表 `相关ASIN` 覆盖 `Asin` 工作表列出的全部 20 个 ASIN，没有额外 ASIN。单个关键词覆盖的竞品数为 1–20，其中 786 个关键词仅关联 1 个竞品，5 个关键词覆盖全部 20 个竞品。

### 19.2 已确认的卖家精灵字段字典

| 原始字段 | 内部字段建议 | 类型 | 导入规则 |
|---|---|---|---|
| 关键词 | keyword_raw / keyword_normalized | 文本 | 必填；保存原文并生成标准化值 |
| 关键词翻译 | keyword_translation | 文本 | 可空；不得因缺失阻止导入 |
| AC推荐词 | ac_recommendation | 原始文本/待解析 | 样表仅 27 条非空；先保留原值，不作为布尔字段直接使用 |
| 流量占比 | traffic_share | 百分比 | 可空；只接受 0–1 数值进入标准字段，异常值保留并告警 |
| 流量词类型 | traffic_types | 多值枚举 | 按 `/` 拆分为自然、SP、品牌、视频、HR、AC 等标签 |
| 预估周曝光量 | estimated_weekly_impressions | 整数 | 允许 0 |
| 相关产品 | related_product_count | 整数 | 用于校验拆分后的相关 ASIN 数量 |
| 相关ASIN | related_asins | 多值文本 | 按 `/` 拆分并建立关键词—竞品多对多关系 |
| ABA周排名 | aba_weekly_rank | 整数 | 可空；排名越小越靠前 |
| 月搜索量 | monthly_search_volume | 整数 | 可空，不得把空值当 0 |
| 月购买量 | monthly_purchase_volume | 整数 | 可空 |
| 购买率 | purchase_rate | 百分比 | 可空，0–1 |
| 展示量 | impressions | 整数 | 可空 |
| 点击量 | clicks | 整数 | 可空 |
| SPR | spr | 数值 | 可空 |
| 标题密度 | title_density | 整数 | 可空 |
| 商品数 | product_count | 整数 | 允许 0 |
| 需供比 | demand_supply_ratio | 小数 | 可空 |
| 广告竞品数 | ad_competitor_count | 整数 | 可空 |
| 点击总占比 | total_click_share | 百分比 | 可空，0–1 |
| 转化总占比 | total_conversion_share | 百分比 | 可空，0–1 |
| PPC竞价 | ppc_bid / ppc_bid_raw | 货币+原文 | 解析美元；样表 1,059 条非空，未发现非法货币值；未来非货币值只存原文并告警 |
| 建议竞价范围 | suggested_bid_min/max/raw | 货币区间 | `-` 表示无有效区间，不转换为 0 |
| #1 前三ASIN | top_asin_1 | ASIN | 可空 |
| #1 点击共享 | top_asin_1_click_share | 百分比 | 可空，0–1 |
| #1 转化共享 | top_asin_1_conversion_share | 百分比 | 可空，0–1 |
| #2 前三ASIN | top_asin_2 | ASIN | 可空 |
| #2 点击共享 | top_asin_2_click_share | 百分比 | 可空，0–1 |
| #2 转化共享 | top_asin_2_conversion_share | 百分比 | 可空，0–1 |
| #3 前三ASIN | top_asin_3 | ASIN | 可空 |
| #3 点击共享 | top_asin_3_click_share | 百分比 | 可空，0–1 |
| #3 转化共享 | top_asin_3_conversion_share | 百分比 | 可空，0–1 |
| 前十ASIN | top_10_asins | 多值 ASIN | 按逗号拆分，保留原顺序 |

### 19.3 样表缺失率与导入影响

- `关键词`、`流量词类型`、`相关产品`、`相关ASIN`、`商品数` 等核心识别字段完整率为 100%。
- `关键词翻译`、`SPR`、`需供比` 等字段完整率约 62.8%。
- `ABA周排名` 完整率为 41.8%。
- `月搜索量`、`月购买量`、`购买率`、`展示量`、`点击量` 完整率均为 37.65%。
- `流量占比` 有 164 条非空，完整率为 8.2%；`AC推荐词` 有 27 条非空；`PPC竞价` 有 1,059 条非空。
- 缺失值必须保存为 `null/未知`，不得自动转为 0，否则会错误影响评分和广告建议。

### 19.4 已发现的数据质量规则

1. 当前真实样表未发现非法 `PPC竞价` 或超出 0–1 的 `流量占比`；导入器仍需对未来文件执行类型校验，异常时保存原文并排除计算。
2. `AC推荐词` 的非空样本不足以证明其为稳定的是/否格式，V1 不对其含义作硬编码推断。
3. `Unique Words` 中存在全角英文及不可见符号，如全角 `ｆａｌｌ`、`ｗｒｅａｔｈ`；本工具词根层需执行 Unicode NFKC 标准化和不可见字符清理，同时保留原词。
4. `相关产品` 应与拆分后的 `相关ASIN` 数量交叉校验；不一致时展示告警但保留原始数据。
5. 文件名、工作表名和主 ASIN 不能作为唯一事实来源，应以 `Asin` 工作表及行级 `相关ASIN` 共同校验。

### 19.5 当前产品分析档案

| 字段 | 内容 |
|---|---|
| 站点 | Amazon US |
| 种子/主 ASIN | 脱敏样例 ASIN |
| 产品 | 示例人造黄杨木花环 |
| 核心属性 | artificial、boxwood、26 inch、large、greenery、waterproof、UV resistant |
| 核心用途 | front door、indoor/outdoor、wall、window、fireplace、entryway |
| 风格/场景 | farmhouse、rustic、traditional、modern、all seasons、wedding、party |
| 可定制元素 | sash、flowers、ribbons、ornaments、seasonal accents |

相关性分析必须使用用户提供的完整标题和五点描述，而不仅使用主 ASIN 或竞品数据。首轮建议核心词根包括 `wreath`、`boxwood wreath`、`artificial wreath`、`greenery wreath`、`front door wreath`；冲突词根和品牌词需从具体关键词中识别，不在 PRD 阶段直接批量否定。

## 20. 广告匹配与否定建议

### 20.1 功能目标

在强、中、弱和不相关的语义判断之外，每个关键词还需要生成可执行但非自动执行的广告建议：

- 建议精准投放；
- 建议广泛投放；
- 暂不投放/观察；
- 建议否定精准；
- 建议否定词组；
- 人工复核；
- 数据不足。

每条建议必须同时展示：建议动作、建议理由、置信度、风险提示、使用的数据和缺失的数据。用户可以接受、修改、驳回或锁定建议。

### 20.2 建议逻辑

| 典型条件 | 默认建议 | 说明 |
|---|---|---|
| 强相关、购买意图明确、属性完整的长尾词 | 精准投放 | 控制搜索意图并集中预算，例如尺寸+材质+用途组合词 |
| 强相关核心短语，有一定拓词空间 | 精准投放 | 先以完整搜索词承接高意向流量，避免未经审核的词组扩展 |
| 强相关或中相关的有效核心词根，语义边界清楚 | 广泛测试 | 仅保留产品核心词根，独立探索广告组、小预算和较低竞价 |
| 强相关核心词根，但希望发现新搜索词 | 广泛测试 | 仅在独立探索广告组、小预算和较低竞价下建议 |
| 中相关、场景或季节匹配，但购买意图较泛 | 观察或广泛测试 | 必须标记为探索，不与核心精准词混组；低搜索量优先观察 |
| 弱相关且缺少足够需求/转化信息 | 观察或低价测试 | 弱相关不等于应立即否定 |
| 完整搜索词明确与产品不符，但其组成词根可能在其他组合中有效 | 否定精准 | 只排除该完整搜索词，避免误杀同词根的有效长尾词 |
| 某词根在本产品语境下普遍不适用，且其所有扩展通常无效 | 否定词组 | 高风险动作，必须展示受影响关键词并要求人工确认 |
| 品牌词、竞品词、歧义词、数据异常或语义置信度低 | 人工复核 | 不自动判断为否定词组 |

### 20.3 决策特征

广告建议至少结合：

- 语义相关性及冲突属性；
- 关键词长度、词根数量和意图明确程度；
- 标题、五点和核心词覆盖；
- 月搜索量和 ABA 周排名（存在时）；
- 20 个竞品中的覆盖数量；
- 流量词类型；
- PPC 竞价与建议竞价范围（解析有效时）；
- 商品数、广告竞品数和需供比；
- 用户设置的品牌词、排除词及不可投放属性；
- 后续导入的广告搜索词表现数据，如点击、订单、花费和转化率。

卖家精灵反查数据只能支持“投放前建议”。在没有用户自己的搜索词报告和转化数据时，系统不得把低购买率、低搜索量或弱相关单独作为否定依据。

### 20.4 否定词安全规则

1. `否定精准` 针对完整搜索词；适用于整词无关，但其中部分词根仍可能有效的情况。
2. `否定词组` 会拦截包含该词组的多个搜索词，风险显著高于否定精准。
3. 系统提出否定词组前，必须列出将受影响的现有关键词数量、样例及潜在强相关词冲突。
4. 若待否定词组出现在任一强相关关键词中，默认阻止批量确认并转为人工复核。
5. 品牌词、竞品词、节日词和场景词默认不自动做否定词组。
6. 所有否定建议均为草稿，只有用户确认后才进入“已批准否定清单”。
7. V1 只导出建议，不直接写入 Amazon Ads。

### 20.5 建议分与置信度

系统分别计算，而不是混成一个黑盒分数：

- `相关性分`：产品与搜索词是否匹配；
- `需求分`：是否存在足够搜索需求；
- `竞争分`：竞价、商品数、广告竞品及集中度；
- `探索价值分`：是否值得用广泛匹配发现长尾；
- `否定风险分`：否定后误伤有效流量的概率；
- `建议置信度`：有效数据覆盖程度及规则一致性。

当月搜索量、ABA、PPC 等关键数据缺失或异常时，降低建议置信度，不以 0 参与计算。

### 20.6 广告建议筛选与导出

词库页新增以下列和筛选项：

- 建议动作；
- 建议匹配方式；
- 建议层级：核心、扩展、探索、观察、否定；
- 建议理由；
- 置信度；
- 否定风险；
- 人工审批状态；
- 人工最终动作；
- 审批备注。

新增导出模板：

- 精准投放候选词；
- 广泛探索候选词；
- 否定精准候选词；
- 否定词组候选词；
- 待人工复核词；
- 已批准广告动作清单。

## 21. 新增验收场景：广告建议

1. 强相关的具体长尾词能够优先得到精准投放建议，并显示命中的产品属性和依据。
2. 有拓展价值的核心词能够得到广泛测试建议，并标记为探索流量；系统不生成词组投放建议。
3. 完整词无关但含有可用核心词根时，系统优先建议否定精准而非否定词组。
4. 系统提出否定词组时，必须显示受影响词清单并拦截与强相关词冲突的操作。
5. 缺少月搜索量、ABA 或 PPC 数据时，系统不得填充为 0，且建议置信度应下降。
6. 用户修改并锁定最终广告动作后，再次导入和重新分析不得覆盖。

## 22. 原文界面图片补充分析

本节来自对参考文章正文中 9 张工具界面截图的逐张检查。截图仅用于理解信息结构和交互方式，不复制其品牌、水印或专有视觉素材。

### 22.1 图片确认的界面结构

1. **匹配强度总览条**：词库顶部以横向分段卡展示全部、强匹配、中匹配、弱匹配及各自数量；点击后立即切换当前词库范围。
2. **统一搜索入口**：搜索框同时支持关键词、翻译和 ASIN，搜索后保留原分组结构并更新组级统计。
3. **分组树表格**：分组以文件夹行呈现，可单独展开/收起；展开后在组内显示关键词明细，而不是跳转到新页面。
4. **组级汇总**：每个分组行展示关键词数量、月搜索量汇总、流量汇总和平均精准度等信息，便于比较分组价值。
5. **关键词行信息**：关键词行同时展示英文词、中文翻译、匹配类型、月搜索量、流量、精准度及操作入口。
6. **批量选择**：分组和关键词均可勾选；顶部显示已选择数量，并提供复制或后续批量动作。
7. **批量展开控制**：提供全部展开、全部收起、全选筛选和清空选择。
8. **排序/归组线索**：关键词行带拖拽把手，表明用户可以调整顺序或归组；本产品 V1 至少支持组内排序，跨组拖拽可在交互稳定后开放。
9. **高级筛选区**：截图展示词库、分组、排序、意图和精准度阈值等组合条件，说明筛选需要分为常用快捷条件和可展开的高级条件。

### 22.2 本产品的适配差异

参考界面只展示强、中、弱相关及基础分组。本产品必须在此基础上增加：

- 多产品切换和产品中心；
- 卖家精灵 33 列指标的可配置展示；
- 20 个竞品 ASIN 覆盖数和来源明细；
- 精准、词组、广泛、否定精准、否定词组广告建议；
- 建议理由、置信度、否定风险和人工审批状态；
- 导入记录、异常数据提示、历史快照及人工结果锁定。

### 22.3 页面布局修订

产品词库工作台采用以下顺序：

1. 产品身份区：产品名称、站点、自有 ASIN/SKU 状态、最近导入时间；
2. 匹配强度统计条：全部/强/中/弱/不相关；
3. 搜索与快捷筛选：关键词、翻译、ASIN及广告动作；
4. 可折叠高级筛选：分类、意图、来源 ASIN、指标范围、置信度、审批状态；
5. 分组树表格：组级汇总与关键词明细同屏；
6. 右侧详情抽屉：来源、历史指标、评分依据、广告建议和人工审批。

### 22.4 图片交互验收

1. 点击强/中/弱统计卡后，分组数量、组级汇总和关键词行同步刷新。
2. 搜索一个词根时，结果仍按分类分组，并只显示包含该词根的长尾词。
3. 展开分组无需页面跳转，收起后保留筛选和选择状态。
4. 选择多个关键词后，顶部准确显示选择数量并启用批量动作。
5. 高级筛选可以组合使用，清除后恢复默认词库范围。
6. 组级月搜索量、流量和精准度必须由当前可见关键词计算，不能展示过滤前旧值。
7. 广告建议列和否定风险不得破坏参考界面的高密度可扫描性。
